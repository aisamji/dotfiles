# Add localhost to all groups and use --limit to select which group to apply
all:
  hosts:
    localhost:
      ansible_connection: local

personal_grp:
  hosts:
    personal:
      ansible_connection: local

work_grp:
  hosts:
    work:
      ansible_connection: local
